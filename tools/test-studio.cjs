const {chromium}=require(process.env.PLAYWRIGHT_PATH||'playwright');
const assert=require('node:assert/strict'),path=require('node:path'),fs=require('node:fs');
const {pathToFileURL}=require('node:url');
(async()=>{const browser=await chromium.launch({headless:true,channel:'msedge',args:['--allow-file-access-from-files']});try{
 const page=await browser.newPage({viewport:{width:1500,height:950}}),errors=[];page.on('pageerror',e=>errors.push(e.message));
 await page.goto(pathToFileURL(path.resolve(__dirname,'../index.html')).href);
 await page.evaluate(()=>{state.duration=60;const c=document.createElement('canvas');c.width=640;c.height=360;const g=c.getContext('2d'),gradient=g.createLinearGradient(0,0,640,360);gradient.addColorStop(0,'#133c47');gradient.addColorStop(1,'#90753e');g.fillStyle=gradient;g.fillRect(0,0,640,360);g.fillStyle='#f2dcb8';g.beginPath();g.arc(420,170,85,0,7);g.fill();addMediaDescriptor({type:'image',url:c.toDataURL(),name:'Cena • referência visual'});Object.assign(selected(),{start:0,end:30});const text=addLayer('text','Uma nova forma de criar.','Título');Object.assign(text,{start:3,end:12,y:77,fontSize:30});const audio=addLayer('audio','','Trilha sonora');Object.assign(audio,{start:0,end:40,mediaDuration:40,sourceOut:40,waveform:Array.from({length:180},(_,i)=>.1+Math.abs(Math.sin(i*1.32)*Math.cos(i*.19))*.8)});state.selected=text.id;renderLayers();syncComposition();setTime(5)});
 await page.evaluate(()=>document.fonts.ready);assert.equal(await page.evaluate(()=>document.fonts.check('12px Inter')),true);
 assert.equal(await page.locator('.topbar>.tools').count(),1);assert.equal(await page.locator('.topbar>.tools').evaluate(e=>getComputedStyle(e).flexDirection),'row');assert.equal(await page.locator('.topbar .brand').isVisible(),false);assert.equal(await page.locator('.topbar .project-name').isVisible(),false);
 assert.equal(await page.locator('#stage>.stage-toolbar').count(),1);assert.equal(await page.locator('#stage>.transport').count(),1);const previewAlignment=await page.evaluate(()=>{const stage=$('#stage').getBoundingClientRect(),wrap=$('.stage-wrap').getBoundingClientRect(),library=$('.library').getBoundingClientRect();return{leftGap:stage.left-wrap.left,widthRatio:stage.width/wrap.width,previewLeft:wrap.left,toolsLeft:library.left,previewWidth:wrap.width}});assert.ok(previewAlignment.leftGap<20&&previewAlignment.widthRatio<.82&&previewAlignment.previewLeft<previewAlignment.toolsLeft&&previewAlignment.previewWidth<900,JSON.stringify(previewAlignment));
 const spacing=()=>page.evaluate(()=>{const a=document.querySelector('[data-second="5"]').getBoundingClientRect(),b=document.querySelector('[data-second="10"]').getBoundingClientRect();return b.left-a.left});
 assert.equal(await spacing(),200);
 await page.setViewportSize({width:1200,height:850});assert.equal(await spacing(),200);await page.setViewportSize({width:1500,height:950});
 assert.equal(await page.locator('.minor-tick').first().textContent(),'');
 const timeline=await page.locator('#timeline').boundingBox();await page.mouse.move(timeline.x+450,timeline.y+50);await page.keyboard.down('Alt');await page.mouse.wheel(0,-180);await page.keyboard.up('Alt');await page.waitForFunction(()=>state.timelineZoom>1);assert.ok(await spacing()>200);
 await page.locator('#previewGrid').click();const grid=await page.locator('.alignment-guides').boundingBox();assert.ok(Math.abs(grid.width/grid.height-9/16)<.01);
 await page.selectOption('#gridAspect','1/1');const square=await page.locator('.alignment-guides').boundingBox();assert.ok(Math.abs(square.width-square.height)<2);
 await page.selectOption('#aspect','9/16');const portrait=await page.locator('#stage').boundingBox();assert.ok(Math.abs(portrait.width/portrait.height-9/16)<.01);await page.selectOption('#aspect','16/9');await page.locator('#previewGrid').click();
 await page.locator('[data-action="split"]').hover();await page.waitForFunction(()=>!document.querySelector('.studio-tooltip').hidden);assert.equal(await page.locator('.studio-tooltip').textContent(),'Dividir no cursor');
 for(const name of await page.locator('[data-icon]').evaluateAll(nodes=>nodes.map(n=>n.dataset.icon)))assert.ok(fs.existsSync(path.resolve(__dirname,`../assets/icons/${name}.svg`)),name);
 assert.ok((await page.locator('.track[data-kind=audio]').boundingBox()).height<(await page.locator('.track[data-kind=video]').boundingBox()).height);
 assert.equal(await page.locator('.tools .tool-label').first().isVisible(),true);
 assert.equal(await page.locator('#splitDestination').count(),0);
 assert.ok((await page.locator('.clip[data-kind="image"]').evaluate(e=>e.style.getPropertyValue('--clip-icon'))).includes('image.svg'));
 assert.deepEqual(await page.locator('.timeline-tool-group').evaluateAll(groups=>groups.map(group=>group.classList[1])),['playback-tools','edit-tools','clip-tools','range-tools','marker-tools','view-tools','timeline-status']);
 const iconCenter=await page.locator('#muteBtn').evaluate(e=>{const style=getComputedStyle(e,'::before');return{position:style.position,left:parseFloat(style.left),top:parseFloat(style.top),width:e.clientWidth,height:e.clientHeight}});assert.equal(iconCenter.position,'absolute');assert.ok(Math.abs(iconCenter.left-iconCenter.width/2)<=1&&Math.abs(iconCenter.top-iconCenter.height/2)<=1,JSON.stringify(iconCenter));
 assert.deepEqual(await page.locator('.track[data-kind=audio] .track-name').evaluate(e=>[...e.children].map(x=>x.matches('.layer-title')?'title':x.hasAttribute('data-lock')?'lock':x.hasAttribute('data-vis')?'visibility':x.hasAttribute('data-mute')?'mute':'selection')),['title','lock','visibility','mute','selection']);
  await page.locator('.track[data-kind=audio] [data-mute]').click();assert.equal(await page.evaluate(()=>state.layers.find(l=>l.type==='audio').muted),true);await page.locator('.track[data-kind=audio] [data-mute]').click();
  await page.evaluate(()=>{setTime(0);$('#timeline').scrollLeft=0});await page.waitForTimeout(20);const timelineOrigin=await page.evaluate(()=>{const corner=$('.ruler-corner').getBoundingClientRect(),zero=$('.major-tick[data-second="0"]').getBoundingClientRect(),handle=$('.time-playhead-handle').getBoundingClientRect(),start=$('.range-start').getBoundingClientRect();return{cornerRight:corner.right,zeroLeft:zero.left,handleLeft:handle.left,startLeft:start.left}});assert.ok(timelineOrigin.zeroLeft>timelineOrigin.cornerRight&&timelineOrigin.handleLeft>timelineOrigin.cornerRight&&timelineOrigin.startLeft>timelineOrigin.cornerRight,JSON.stringify(timelineOrigin));await page.evaluate(()=>setTime(5));
  await page.mouse.move(5,5);await page.screenshot({path:path.join(require('node:os').tmpdir(),'motion-studio-check.png')});
  await page.evaluate(()=>{for(let i=0;i<12;i++){const l=addLayer('text',`Conteúdo ${i}`,`Camada adicional ${i}`);Object.assign(l,{start:i/2,end:20+i/2,trackId:`scroll-${i}`})}renderLayers();const timeline=$('#timeline');timeline.scrollTop=160;timeline.scrollLeft=140});await page.waitForTimeout(50);
  const scrollChrome=await page.evaluate(()=>{const timeline=$('#timeline').getBoundingClientRect(),ruler=$('.time-ruler').getBoundingClientRect(),corner=$('.ruler-corner').getBoundingClientRect(),handle=$('.time-playhead-handle').getBoundingClientRect(),line=$('#playhead');return{cornerText:$('.ruler-corner span').textContent,cornerLeft:corner.left,timelineLeft:timeline.left,cornerTop:corner.top,rulerTop:ruler.top,handleTop:handle.top,handleBottom:handle.bottom,rulerBottom:ruler.bottom,lineHeight:parseFloat(line.style.height),scrollHeight:$('#timeline').scrollHeight}});assert.equal(scrollChrome.cornerText,'Camadas');assert.ok(Math.abs(scrollChrome.cornerLeft-scrollChrome.timelineLeft)<2&&Math.abs(scrollChrome.cornerTop-scrollChrome.rulerTop)<2,JSON.stringify(scrollChrome));assert.ok(scrollChrome.handleTop>=scrollChrome.rulerTop&&scrollChrome.handleBottom<=scrollChrome.rulerBottom,JSON.stringify(scrollChrome));assert.ok(scrollChrome.lineHeight>=scrollChrome.scrollHeight-2,JSON.stringify(scrollChrome));
  await page.screenshot({path:path.join(require('node:os').tmpdir(),'motion-scroll-check.png')});
  // Real PCM fixture verifies the file import and waveform decoder, not only the drawing.
  const count=16000,wav=Buffer.alloc(44+count*2);wav.write('RIFF',0);wav.writeUInt32LE(wav.length-8,4);wav.write('WAVEfmt ',8);wav.writeUInt32LE(16,16);wav.writeUInt16LE(1,20);wav.writeUInt16LE(1,22);wav.writeUInt32LE(16000,24);wav.writeUInt32LE(32000,28);wav.writeUInt16LE(2,32);wav.writeUInt16LE(16,34);wav.write('data',36);wav.writeUInt32LE(count*2,40);for(let i=0;i<count;i++)wav.writeInt16LE(Math.round(Math.sin(i*2*Math.PI*440/16000)*10000),44+i*2);
  await page.locator('#audioInput').setInputFiles({name:'onda-teste.wav',mimeType:'audio/wav',buffer:wav});await page.waitForFunction(()=>state.layers.some(l=>l.name==='onda-teste.wav'&&l.waveform?.some(v=>v>0)));
  assert.ok(await page.evaluate(()=>state.mediaLibrary.some(m=>m.name==='onda-teste.wav'&&m.waveform.length)));
 assert.deepEqual(errors,[]);console.log('PASS: local font/icons, fixed ruler after resize, zoom, guides, portrait aspect, tooltips, compact tracks');
}finally{await browser.close()}})().catch(e=>{console.error(e);process.exitCode=1});
